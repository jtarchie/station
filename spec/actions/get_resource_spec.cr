require "../spec_helper"

include Station

describe Station::Actions::GetResource do
  it "uses a destination directory" do

  end
  it "uses the params"
  it "uses the version provided"
  it "uses source"
end
